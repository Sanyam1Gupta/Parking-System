/* import EMAIL_AND_DOWNLOAD_ACTION_TYPES from '../actionTypes/EmailAndDowloadActionTypes';

const DEFAULT_STATE = {};

const EmailAndDownloadReducer = (state = DEFAULT_STATE, action) => {
  const { type, data } = action;
  switch (type) {
    case EMAIL_AND_DOWNLOAD_ACTION_TYPES.FETCH_EMAIL_REPORT_SUCCESS:
      return { ...state, ...data };
    case EMAIL_AND_DOWNLOAD_ACTION_TYPES.FETCH_DOWNLOAD_REPORT_SUCCESS:
      return DEFAULT_STATE;
    default:
      return state;
  }
};

export default EmailAndDownloadReducer; */
